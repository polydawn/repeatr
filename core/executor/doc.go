/*
	Executor implementations are responsible for machining inputs into outputs.

	Executors are command invocations, typically backed by either containers or VMs.

	Imports: executors import from the `input` and `output` package implementations
	(never the other way around).
*/
package executor
