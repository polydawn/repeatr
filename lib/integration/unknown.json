{
    "inputs": [
        {
            "type": "exec-tar",
            "mount": "/",
            "silo": ["assets/ubuntu.tar.gz"]
        }
    ],
    "action": {
        "command": [ "echo", "Hello from repeatr!" ]
    },
    "outputs": [
        {
            "type": "tar",
            "mount": "/var/log",
            "silo": ["file://basic.tar"]
        }
    ]
}
